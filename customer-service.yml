spring:
  application:
    name: customer-service
  datasource:
    url: jdbc:mysql://localhost:3306/customers_db
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

